import styled from 'styled-components';

const Avatar = styled.img`
  width: 10vmin;
  max-width: 50px;
  height: 10vmin;
  max-height: 50px;
  border-radius: 50%;
  margin: .4rem;
`;

export default Avatar;
